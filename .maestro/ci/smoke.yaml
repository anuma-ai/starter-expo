appId: dev.privy.example
---

- launchApp
- waitForAnimationToEnd

# Verify the login screen renders with email input and buttons
- assertVisible: "Continue with email"
- assertVisible:
    id: "email-input"
- assertVisible: "Sign in with Google"
